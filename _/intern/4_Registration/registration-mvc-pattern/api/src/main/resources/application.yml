spring:
  application:
    name: api
  profiles:
    active: local, test, prod
  datasource:
    driver-class: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: okulhanmuratbekov@gmail.com
    password: njya dzcr btyf yppw
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true
        default-encoding: UTF-8
        from: okulhanmuratbekov@gmail.com
server:
  port: 5556

